/*
 * Resource identifiers shared between the Win32 app source and app.rc.
 *
 * Keep this header dependency-free so it stays usable from both `cl` and
 * the resource compiler (`rc`).
 */
#ifndef QSORIPPER_WIN32_APP_H
#define QSORIPPER_WIN32_APP_H

#define IDI_APP_ICON 101

#endif /* QSORIPPER_WIN32_APP_H */
